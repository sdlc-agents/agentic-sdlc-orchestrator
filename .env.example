# Only needed for --mode openai. The default --mode mock runs the whole
# platform deterministically with no API key and no network access.
OPENAI_API_KEY=sk-...

# Optional: override the model used by --mode openai.
ASEP_MODEL=gpt-4o-mini
